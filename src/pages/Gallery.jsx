import { motion } from 'framer-motion'
import { services } from '../data'
import { MotionSection, PageHero, SpotlightCard, BlurText } from '../components/shared'

export default function Gallery() {
  const images = services.map(service => [service.image,service.title])
  return <main id="main-content" tabIndex="-1"><PageHero eyebrow="Galeri" title={<><BlurText text="İşin içinden," animateBy="words" delay={100} /> <br/><em><BlurText text="üretimin ritminden." animateBy="words" delay={500} /></em></>} copy="Temsili görseller, yayın öncesinde gerçek tesis, ekip ve üretim fotoğraflarıyla değiştirilmelidir."/><MotionSection className="section"><div className="container gallery-next">{[...images,...images].map(([src,label],index)=><motion.div key={`${label}-${index}`} initial={{ opacity:0,scale:.97 }} whileInView={{ opacity:1,scale:1 }} viewport={{ once:true }} transition={{ delay:(index%6)*.05 }} style={{ display: 'grid' }}><SpotlightCard className="gallery-figure" style={{ position: 'relative', minHeight: '280px', margin: 0, overflow: 'hidden', background: 'var(--ink-soft)', borderRadius: 'var(--radius-md)', boxShadow: 'var(--shadow-soft)', border: '1px solid var(--line)', height: '100%' }} spotlightColor="rgba(209,133,73,0.12)"><img style={{ width: '100%', height: '100%', objectFit: 'cover', filter: 'saturate(.85) contrast(1.05)', transition: 'transform .8s cubic-bezier(.2,.7,.2,1)' }} src={src} alt={`${label} için temsili üretim görseli`} loading="lazy"/><figcaption style={{ position: 'absolute', inset: 'auto 0 0', padding: '3rem 1.5rem 1.2rem', color: 'var(--white)', background: 'linear-gradient(transparent, rgba(0,0,0,.85))', font: '600 .7rem "DM Mono", monospace', letterSpacing: '.16em', textTransform: 'uppercase', zIndex: 2 }}>{label}</figcaption></SpotlightCard></motion.div>)}</div></MotionSection></main>
}
